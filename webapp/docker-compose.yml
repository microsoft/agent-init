services:
  app:
    build:
      context: ..
      dockerfile: Dockerfile.webapp
    ports:
      - "${HOST_PORT:-3000}:3000"
    env_file:
      - .env
    volumes:
      - reports-data:/app/data
volumes:
  reports-data:
